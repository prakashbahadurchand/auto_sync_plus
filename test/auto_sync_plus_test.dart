import 'package:auto_sync_plus/auto_sync_plus.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('testing auto sync plus features', () {
    final asp = AutoSyncPlus();
    //expect(asp.addOne(2), 3);
    //expect(asp.addOne(-7), -6);
    //expect(asp.addOne(0), 1);
  });
}
